[workspace]
resolver = "2"
members = ["crates/config", "crates/domain", "crates/leptos-utils", "gateway", "live-search", "i18n-demo", "e2e-tests"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
# Edition 2024 requires Rust 1.85+. We pin to 1.94 in `rust-toolchain.toml`
# so all contributors build with the same toolchain (and wasm32 target).
rust-version = "1.85"

[workspace.dependencies]
anyhow = "1.0"
base64 = "0.22"
axum = "0.8"
chrono = { version = "0.4", features = ["serde"] }
console_error_panic_hook = "0.1"
futures = "0.3"
gloo-net = { version = "0.7", features = ["eventsource"] }
jsonwebtoken = { version = "10", default-features = false, features = ["aws_lc_rs"] }
leptos = "0.8"
leptos_axum = "0.8"
leptos_i18n = "0.6"
leptos_meta = "0.8"
leptos_router = "0.8"
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls", "stream"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# sqlx 0.9 has no built-in `tracing` feature. `sqlx-otel` would be the
# natural drop-in for per-query spans, BUT `sqlx-otel 0.3.0` predates sqlx 0.9
# and pulls in `sqlx 0.8.6` + `opentelemetry 0.31.0` alongside the workspace's
# `sqlx 0.9` + `opentelemetry 0.32`. The dep is kept here as a placeholder for
# the eventual 0.9-compatible release — do NOT enable `--features otel` on
# `live-search` until upstream ships a compatible version, or until you verify
# your OTel collector accepts the dual-versioned tree.
sqlx = { version = "0.9", features = ["postgres", "runtime-tokio", "tls-rustls", "json", "macros", "migrate", "chrono", "uuid"] }
subtle = "2.6"
thiserror = "2.0.18"
tokio = { version = "1.52", features = ["full"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tokio-util = { version = "0.7", features = ["rt"] }
tower = "0.5"
tower-http = { version = "0.7", features = ["trace", "cors", "fs", "timeout"] }
tracing = "0.1"
tracing-subscriber = "0.3"
uuid = { version = "1.23", features = ["serde"] }
wasm-bindgen = "0.2"
axum-prometheus = "0.10"
tower_governor = { version = "0.8", features = ["axum"] }
axum-valid = "0.24"
validator = { version = "0.20", features = ["derive"] }
aws-lc-rs = "1"
moka = { version = "0.12", features = ["future"] }
stylance = "0.8"
leptos-use = "0.19"
leptos-struct-table = "0.19"
lepticons = "0.13"
# `bitcode` feature required for leptos-forms-rs to work with Leptos 0.8.11+ server_fn Bitcode encoding.
leptos-forms-rs = { version = "1", features = ["bitcode"] }
utoipa = "5"
utoipa-axum = "0.2"
# `axum` enables the SwaggerUi router builder; `vendored` bundles Swagger UI assets so Docker builds don't need network.
utoipa-swagger-ui = { version = "9", features = ["axum", "vendored"] }
opentelemetry = "0.32"
opentelemetry_sdk = { version = "0.32", features = ["rt-tokio"] }
opentelemetry-otlp = { version = "0.32", features = ["reqwest-client", "http-proto", "reqwest-rustls"] }
tracing-opentelemetry = "0.33"
axum-tracing-opentelemetry = "0.38"
sqlx-otel = "0.3"
console-subscriber = "0.5"
criterion = { version = "0.8", features = ["html_reports"] }
insta = "1"
proptest = "1"
testcontainers = "0.27"
testcontainers-modules = { version = "0.15", features = ["postgres"] }
# Layered config: TOML file + RWF_* environment variable overrides.
# The `config` crate gives us typed deserialization + per-source priority
# without needing unsafe or hand-rolled merge logic.
config = { version = "0.15", default-features = false, features = ["toml"] }

# Shared domain types (no framework dependencies).
rwf-domain = { path = "crates/domain" }

# Shared Leptos axum helpers (currently the doubled-prefix server-fn probe).
# rlib only: the probe uses leptos_axum::handle_server_fns (SSR-only).
leptos-utils = { path = "crates/leptos-utils" }

[workspace.lints.rust]
unsafe_code = "deny"
rust_2024_compatibility = { level = "deny", priority = -1 }
missing_debug_implementations = "warn"
missing_docs = "allow"

[workspace.lints.clippy]
pedantic = { level = "deny", priority = -1 }
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
todo = "deny"
unimplemented = "deny"
too_long_first_doc_paragraph = "allow"
nursery = { level = "warn", priority = -1 }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.bench]
inherits = "release"
debug = true
strip = false

[profile.dev.package."*"]
opt-level = 3
